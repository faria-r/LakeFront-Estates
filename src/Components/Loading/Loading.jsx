import React from 'react';

const Loading = () => {
    return (
        <div>
            <span className="loading text-3xl loading-spinner text-error"></span>
        </div>
    );
};

export default Loading;